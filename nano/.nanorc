# Editing niceties
set autoindent
set tabsize 4
set mouse
set nohelp
set zero

# Quit without the save prompt: ^X exits and auto-answers No, discarding changes
bind ^X "{exit}n" main

# Syntax highlighting from the system definitions
include /usr/share/nano/*.nanorc
